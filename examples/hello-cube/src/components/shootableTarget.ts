import { defineComponent } from '@arcane-engine/core';

/** Marks shootable targets whose destruction increments {@link GameState}.kills. */
export const ShootableTarget = defineComponent('ShootableTarget', () => ({}));
